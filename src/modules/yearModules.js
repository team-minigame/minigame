exports.checkYear = function(inputYear) {

	inputYear = inputYear.equal(num)
	inputYear = inputYear.equal(4)
	
	assert.isAbove(inputYear1, inputYear2, 'inputYear1 is after inputYear2');
	assert.isBelow(inputYear1, inputYear2, 'inputYear1 is before inputYear2');

	var output = ;

	return output;
}

